# Private configuration files

Put any private or lab-internal paths here.

Typical example:
- `base_xml_path.txt` containing the absolute path to the private MuJoCo XML file.

Example contents:

```text
/home/yourname/private/spirob/xml/2Dsimreal_grasp.xml
```

This file should stay local and should not be committed.
